# skill.md - 開発エージェント向け作業ルール

このプロジェクト（研究アイデア支援システム）で修正・追加などの作業を行う際の共通ルール。
`AGENTS.md` とあわせて常に考慮すること。

## 1. 共通ルール

- 作業前に必ず `AGENTS.md` と `skill.md` を読むこと。
- 変更対象のコード規約・既存パターンは `AGENTS.md` に従うこと。

## 2. 修正後「毎回」コミットとpushを確認する

**ファイルを修正・作成・削除したときは、作業完了時点で必ず次の対応を行うこと。**

1. 作業が完了したら、その旨をユーザーに報告してから、
   解釈せず **明示的に** 以下の確認を行う:

   > 「コミットしてpushしますか？」

2. ユーザーの指示に従う:
   - 指示があり「はい」= コミット + push を行う
   - 「いいえ」/保留 = コミットしない。pushもしない。
   - 指示がない/不明 = コミットしてよいか確認し、確認できるまでコミットしない。

## 3. 禁止事項

- ユーザーの明示的な指示なしでコミット・pushしないこと。
- 無断の amend / force push / GitHub上の設定変更をしないこと。
- 不必要なファイルを no でコミット対象に含めないこと。
  （シークレット・設定ファイル等は含めないよう注意すること）